{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension Nest Demo",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/dist/**/*.js"
      ],
      "preLaunchTask": "Extension Nest: build"
    }
  ]
}
