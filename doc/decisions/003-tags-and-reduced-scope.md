# 003：独立 Tag 与功能范围收敛

> 2026-09-16 范围更新：当前以 [决策 004](../decisions/004-public-vsix-discovery.md) 为准。本页保留历史调查/实现记录，启停、更新及完整安装清单要求不再作为活动首版门槛；新历史标记未由旧证据证明实现。


日期：2026-09-14。依据用户明确要求生效；替代 002 中配置导入导出、卸载及无 Tag 的旧范围。

- Group 仍为唯一归属；Tag 可多个，只参与搜索和筛选，不创建组，不影响组计数。
- Tag 编辑只改 tags，移动或删除 Group 保留 Tag；多个 Tag 筛选取交集，并与 Group、状态、关键词取交集。
- 不提供配置导入导出，不提供插件卸载命令或入口；对用户在原生 VS Code 中进行的外部安装/卸载仍可只读同步。
- 保留 PRD-010/F10、PRD-014/F14 撤销占位，不复用编号；新增 PRD-017/F17。
- 配置保存在本地；旧 Demo 快照缺少 tags 时补空数组，不重置已有组或插件状态。
- 用户确认后已将 demo/src/Sidebar.tsx 和两张旧设计图片移入回收站。其历史内容可通过首次提交 3143ea8 追溯，历史归档不代表文件仍存在。

本次不更新版本，不修改 CHANGELOG，不自动提交。实现和验证结果见 PRODUCT，剩余正式产品任务见 TODO。

## 2026-09-15：首版仅管理本地插件

用户确认首版只覆盖当前 Profile 的本地插件。Remote 清单聚合、Local/Remote 实例选择、跨目标 Mixed 状态和 Remote 断连恢复延后，不作为首版发布门槛。连接 Remote 的窗口也不聚合远端插件。当前 Profile 的完整本地清单、真实启用状态、Workspace/全局操作范围、待重启状态、Profile 隔离和多窗口冲突处理仍需实现与验证。本决定优先于既有模块中更宽的跨环境要求，不代表 Demo 已接入真实清单。
