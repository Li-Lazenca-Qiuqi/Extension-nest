# 当前待办

更新日期：2026-09-17。唯一活动执行清单。基于 0.2.1（78b47e1）的 [全部 PRD 审查](note/prd-audit-2026-09-17.md) 整理；[原清单归档](archive/2026-09-17-before-prd-audit.md) 仅供追溯。

公开发现、历史缓存、隐藏内置、分组、Tag、手动与字段排序、原生导航、旧占位永久清理已接入。Dashboard 默认 Visible，用户界面统一 Not found。已有 115 项测试及本地 VSIX 干净安装证据；主体实现不等于全部场景已验收。

全部 PRD 已移入 [历史归档](archive/2026-09-17-prd/000-overview.md)，不再作为活动规范。当前产品范围由 PRODUCT 与决策 004 维护，本文件完整承接未完成任务及验收标准；PRD/F/A 编号仅用于追溯。勾选须记录环境、操作、预期与实际结果及证据位置，不能仅凭代码存在或旧测试数量判定通过。

## 一、待实现或修复（建议顺序）

- [x] GAP-01（2026-09-18 需求调整）：撤销“默认 Visible 保留所有空组”的要求。All 无其他筛选时显示空组，其他筛选隐藏无匹配成员的组；明确选择该组或搜索命中组名时保留空分区与拖放入口。组名按去空白、大小写不敏感的包含匹配；Group 选择仍限制范围。投影与 App 交互回归已覆盖。
- [x] GAP-02（2026-09-18）：顶部仅 All/Visible/Not found，Ungrouped 归入右侧 Group 筛选；组与可见性独立组合，All 清空全部条件。App 原生消息集成回归及浏览器组合筛选通过；完整统计矩阵仍见 VERIFY-11。
- [x] GAP-03（2026-09-19）：保留 extensionOrder 已保存顺序，其余项按名称、同名按 ID 升序追加；投影不改组织与发现缓存。回归覆盖扫描顺序变化、手动顺序、刷新/重启、历史消失重现及隐藏内置项。
- [ ] GAP-04（PRD-009；原 STORE-03）：提供损坏组织配置/发现缓存的独立修复交互，保留另一份有效数据，丢弃数据前明确确认；不改变旧占位清理无备份的要求。
- [ ] GAP-05（PRD-015；原 SYNC-01/02）：统一刷新触发合并，补上下文代次与请求失效控制，或验证等价宿主生命周期方案；覆盖异步保存期间释放及 Profile 切换/切回，不伪造身份。
- [ ] GAP-06（PRD-001/011/016；原 UI-10）：区分成功无当前发现但有历史、搜索无匹配、首次空清单与失败；提供相应历史查看/重试入口。
- [ ] GAP-07（PRD-016）：展示最近成功扫描时间，Stale/Error 保留上次成功时间，统计与缓存新鲜度一致。
- [x] GAP-08（2026-09-18）：接入 VS Code 显示语言，补齐命令/设置、原生树与提示、Dashboard 的中文与英文文案；技术术语和用户数据保持原样，其他语言回退英文。133 项测试、类型检查与构建通过，含中英渲染、动作值不变、Webview 语言与 CSP、命令资源完整性回归；浏览器中文筛选、排序、创建与校验提示已检查，真实宿主切换见 VERIFY-14。

## 二、已有实现，待补验收证据

- [ ] VERIFY-01（原 STORE-04/QA-04）：运行中切 Profile/切回、复制/继承资源 Profile、Remote 窗口本地 UI 宿主放置；说明其他发行目录及 macOS/Linux 未测范围。
- [ ] VERIFY-02（原 STORE-05）：真实双 VS Code 窗口的单写者、只读提示、存储传播、退出/重载接管，不以进程互斥测试代替全部验收。
- [ ] VERIFY-03（原 UI-01/03/05/06/07/09、QA-01）：真实宿主分组创建/重命名/删除确认、多选移组/排序、Tag 单个/批量替换、取消/失败无部分写入、重启保存；覆盖历史重现和隐藏内置数据保护。
- [ ] VERIFY-04（原 UI-07/08）：拖放取消/失焦/目标失效/非法载荷、筛选落点和边缘滚动；树展开/选择/滚动保持；原生导航 Opened/Cancelled/Failed 与 Copy ID，不把导航当管理成功。
- [ ] VERIFY-05（原 QA-02）：补测真实 VS Code 连续拖动窗口宽度、跨工具栏换行断点的卡顿；已移除菜单测宽监听与状态回写，尚未完成原生缩放性能复验。100 个已知 ID、20 组、30 次预热 p95<100 ms，整理反馈<200 ms，快照就绪首屏<1 s，可用通知后同步<2 s。
- [ ] VERIFY-06（原 QA-03/05）：Light/Dark/High Contrast、键盘辅助、窄面板、图标失败回退；CSP、资源范围、消息/ID/拖放校验与纯文本渲染复核。
- [ ] VERIFY-07（原 REL-02）：现有 0.2.1 VSIX 的实际安装后 UI 与升级后组织/历史保留；干净隔离安装和版本枚举已通过，不重复列为未完成。

- [ ] VERIFY-08（A01/A21/A26/A27）：发现与历史回归。50 个本地公开可见唯一夹具（含未激活项）全部发现，初次 Ungrouped=50，不调用 activate、不按 isActive 筛选；未曾发现的禁用项不虚构。已分组/加 Tag/排序项消失、重启、重现后元数据不丢、不重复；成功空快照与首次错误/后续 Stale、部分非法快照和保存失败区分，失败不批量标缺失、不推进成功时间。复用已有夹具证据并补缺失场景。
- [ ] VERIFY-09（A15/A29）：持久化与迁移回归。组织/缓存独立，缺失配置可初始化，损坏/未知版本/非法 Tag 不静默清空；只迁移实际旧 Demo 组织，缺 tags 补 []，不迁移 seed 或伪造发现时间。组织 ID 无真实历史仅显示 Not found；重启/Workspace 切换保留组色、顺序、归属、Tag 和历史。写失败不半完成，恢复流程覆盖组织与缓存分别损坏。
- [ ] VERIFY-10（内置隐藏与旧记录清理）：内置 ID 默认从两视图/搜索/统计隐藏，切换设置恢复；不按 publisher 前缀过滤，用户目录中的同 ID 更新版仍隐藏，隐藏项归属/Tag/顺序不被普通编辑覆盖，删组包含隐藏成员。目录读取失败保留旧状态并报错。两处清理入口数量一致；预览精确 ID/组/Tag、多选确认后永久删除，无备份/恢复；真实历史、可见项、隐藏内置不参与；只读/未就绪不可用，取消不写、预览后数据变化整批取消、写失败保留，重启不从旧 Demo 复活。
- [ ] VERIFY-11（A18/A30）：搜索/统计组合矩阵。名称/ID/publisher/Tag 大小写不敏感搜索，Group（含 Ungrouped）、Visible/Not found、多 Tag AND 交集；All 清空条件。按默认隐藏规则后的唯一已知 ID 计数，All=Visible+Not found，Ungrouped 是 All 的子集，组标题按筛选后计数；空、仅历史、旧占位、重现、失败与组合筛选均不重复计数、不写组织数据；过期统计有明确标志，不称 Installed/Enabled。
- [ ] VERIFY-12（A09/A19/A28）：同步触发与发布。启动、公开扩展事件、窗口聚焦、同窗口切回 Dashboard 标签、手动 Refresh 均补读并合并，避免反馈循环；延迟请求、切 Profile/切回及异步保存中退出不发布旧结果。成功持久化后两视图发布同一快照；写失败保留旧快照并可重试，发现刷新不覆盖整理动作；只读会话发现不冒充跨重启保存。
- [ ] VERIFY-13（PRD-002/011 局部 AC、A22）：原生树互斥覆盖全部未隐藏已知 ID，空组/Ungrouped 保留，刷新尽量保持展开/选择/滚动；点击组联动 Dashboard 该组全部记录，重复打开仅聚焦同一面板。新组立即提供空落点、不弹选插件引导，鼠标可完成创建/选择/移组。Dashboard 默认 Visible，空组仅在 All 无筛选、明确选组或组名搜索命中时展示；分组展开、Ungrouped 置后，全部展开/收起、单/双栏及 ≤760px 单栏回退；无网页侧边栏、复选框、Move 弹窗、自建 Details 或正常卡片 Visible 标签。Not found 非仅颜色标记且可整理，历史版本标 Last seen version，无可靠元数据只用占位。

- [ ] VERIFY-14（本地化）：在真实 VS Code 中分别使用中文、英文显示语言并重启，核对命令面板、设置、原生树、创建/重命名/删除/清理提示、Dashboard 筛选/排序/标签/状态/错误及辅助标签；窄面板不溢出，切换语言不改组名、Tag、归属、顺序与发现历史。自动化翻译及渲染回归不能代替真实宿主语言切换验收。

### VERIFY-03/04 的具体通过条件

以下是上述验收任务的组成部分，不建立第二份勾选清单。

| 任务 | 必须覆盖的边界 |
| --- | --- |
| VERIFY-03 分组（A02/A05/A06） | 组名 trim、非空、50 Unicode 字符上限、大小写去重、Ungrouped 保留名、特殊字符纯文本；重命名保持 ID/顺序/归属，取消不写；删除确认后包括隐藏/历史成员回 Ungrouped，Tag/历史不变。50 夹具中分配 4 项后目标组=4、Ungrouped=46，重启保持 |
| VERIFY-03 移动/排序（A03/A04/A16） | Ctrl/Command 多选来自不同组的项，一次提交唯一归属且保留 Tag；同组无新落点幂等，有落点只改顺序；顶部 Sort 按组名升降序后重启保持、同名按 ID 稳定排序，组拖动与 Move Up/Down 不越界、不嵌套，Ungrouped 固定；插件 name/publisher 升降序（移除 ID 排序入口）、上下移动、同名稳定排序、组首/组尾/网格/筛选锚点、多选相对顺序及重启保存，未选/隐藏项顺序不被重排 |
| VERIFY-03 Tag（A25） | trim/忽略空值、大小写去重保留首次文本、每项≤30 Unicode 字符/每扩展≤10 项，超限整批拒绝；单个编辑保留归属/历史，批量先展示数量和完整数组、采用替换语义；取消与错误无部分写入 |
| VERIFY-04 拖放（A17） | 标题/成员/内容区/空组一致落点、高亮与插入线；无节点空白、失效目标、非法外部载荷不写不执行；0.82 同尺寸单一指针预览、起拖偏移、无系统双重拖影，按钮/Tag/菜单不启动拖放，离开/放下/取消/失焦清理；六点组抓手及方向键辅助 |
| VERIFY-04 导航（A23） | 当前/历史/旧占位、无市场条目、取消、命令失败和恢复入口；图标/标题导航，Copy ID 完整，Ctrl/Command 选择不误触导航；Opened/Cancelled/Failed 不改变组织数据、不宣告管理成功 |
| VERIFY-06 安全与可访问性 | 三主题文本/焦点/状态可读，窄面板无溢出；资源范围和 CSP 限制、消息动作与 ID/拖放校验、纯文本防注入；不执行任意命令/URI，不读私有数据库或安装登记；正式入口无 seed、模拟管理、Reset demo |
| VERIFY-09 数据格式 | schemaVersion=1；groups 稳定 ID、六位十六进制颜色、唯一非负安全整数 order；assignments 指向有效唯一组，缺归属即 Ungrouped；tags 为字符串数组；extensionOrder 为去重小写 ID。大小写重复映射拒绝，缓存 source/时间/ID 与元数据一致，firstSeenAt≤lastSeenAt，组织数据不混入启停/更新事实 |

## 三、正式发布前待决定

- [ ] RELEASE-01（原 REL-01）：确认正式包名/显示名、License 和最低 VS Code 版本。当前仅本地交付 0.2.1，尚未上架或推送；不自动发布。

## 验收编号覆盖与执行顺序

先完成尚未勾选的 GAP 项，再补对应回归；已有可复用证据不重复当作零进度，下面列的是仍需整体核验的条件。

| 历史验收编号 | 当前任务 |
| --- | --- |
| A01、A21、A26、A27 | VERIFY-08、GAP-06 |
| A02、A03、A04、A05、A06 | VERIFY-03、VERIFY-04、GAP-01 |
| A09、A19、A28 | VERIFY-01、VERIFY-12、GAP-05 |
| A15、A29 | VERIFY-09、GAP-04 |
| A16、A17 | VERIFY-03、VERIFY-04、GAP-01、GAP-03 |
| A18、A30 | VERIFY-11、GAP-02、GAP-06、GAP-07 |
| A22 | VERIFY-06、VERIFY-13 |
| A23 | VERIFY-04 |
| A24 | VERIFY-02 |
| A25 | VERIFY-03、VERIFY-11 |
| PRD-002-AC01、PRD-002-AC02、PRD-011-AC01、PRD-011-AC02 | VERIFY-13、GAP-01 |
| 无旧编号的补充条件 | VERIFY-05 性能、VERIFY-07 安装升级、VERIFY-10 隐藏/清理、RELEASE-01 发布决策 |

共承接 22 个有效 A 编号和 4 个局部 AC；A07/A08/A10/A11/A12/A13/A14/A20 撤销，不复用，也不作为待验收。

## 范围边界

导入导出、启停、更新、卸载、自建 Details 已撤销，不属于欠缺功能。收藏、自定义图标、Settings Sync、Remote 聚合、其他 Profile 管理和规则分类仅为后续方向。

README、安装说明与 0.2.1 CHANGELOG 已在授权发布任务中同步（原 REL-03），不再作为待完成项。后续完成记录定期归档；产品要求与本表状态分开维护。
