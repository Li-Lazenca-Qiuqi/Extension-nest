# 普通 VSIX 范围调整前的执行清单

此页仅供追溯，不作为当前要求。旧基线为 Git 提交 681f655；旧 PRD 可在该提交中查看。当前范围见 [决策 004](../decisions/004-public-vsix-discovery.md)。旧 M0 完整安装、启停及更新接口门槛因用户调整范围撤销，不标为实现完成。F08-01 已完成记录保留在下方快照。

以下为原 TODO 原文，内部相对路径以原 doc/TODO.md 为基准。

```markdown
# 当前待办

更新日期：2026-09-16。此文件是唯一活动执行清单。2026-09-15 逐项核对 PRD-000、15 个有效功能模块及当时工作区；完成项按实际证据勾选，定期归档，不以 Demo、自动测试或构建通过代替正式产品验收。

## 当前基线与执行顺序

- 当前版本 0.2.0，publisher 已设为 Lazenca，市场图标已生成；本轮交互修改尚未提交，不重复列为待开发功能。
- 已有交互：原生分组树、Dashboard 单/双栏、全部展开/收起、组配色、卡片图标、Ctrl 多选、拖动移组与卡片落点排序、分组六点抓手与宽松落点、字段排序、空组占位、Tag 编辑和筛选、点击图标/标题打开原生扩展页。
- 用户已确认卡片拖动正常；最新分组落点修改有自动测试证据，真实宿主手感及完整边界验收仍需补齐。当前 63 项测试、类型检查及构建通过；证据见 [Demo 实现说明](note/demo-implementation.md)。
- 宿主仍读取 `extensionNest.demo.state` 与示例清单，Enabled/Update 仍修改 Demo 状态。读取真实图标和打开原生扩展页不代表已接入真实管理。
- 下次依次完成：**M0 数据通路验证 → 正式状态模型与保存 → 真实清单/管理/同步 → 现有交互接入 → 验收与交付**。本次仅记录，不继续开发、不提交、不更新版本。

## 已确认范围与不再开发的内容

首版仅管理**当前 Profile 的本地插件**。完整本地清单、Profile 隔离、全局/Workspace 启停范围仍须完成；Remote 相关事项单列延后，不能混进首版门槛。依据 [范围决策](decisions/003-tags-and-reduced-scope.md)及本轮用户修订。

以下已撤销，不作为未完成功能重新实现：

- PRD-010/F10 配置导入导出、PRD-014/F14 卸载入口、A12–A14；外部原生安装/卸载的只读同步仍保留。
- 自建 Details 弹窗及 Details 菜单；保留点击插件图标/标题打开原生页和 Copy ID。
- 第 9 项 Move 提示，以及 Move 按钮、移组菜单、目标组弹窗、原生 moveToGroup 命令；移组仅通过拖动，Ctrl 多选后可批量拖动。
- 多选复选框、创建组后引导添加插件、重复 Status 下拉框、Check updates 按钮。更新查询改为打开面板自动执行一次。

## M0：必须先验证的数据与原生操作通路

2026-09-16 进展：已确认本机 VS Code 1.137.0 的版本与源码，新增只读宿主诊断命令及可复现静态检查脚本；67 项测试、类型检查和构建通过。发现 `checkForUpdates` 包含其他插件更新调用，不能作为只读查询；完整本地清单及真实启停/更新状态仍缺少已验证的可维护读取通路。详见 [M0 调查与宿主采集步骤](note/m0-native-capabilities.md)。以下六项均保持未完成，不以静态调查或诊断工具代替宿主验收。

后续实测：8 个隔离真实宿主场景通过，包含 50 个夹具的 CLI 逐 ID 对照、禁用项 API 漏项、重启/Workspace 保存、命名 Profile 隔离及本地 VSIX 安装/升级事件。工程检查更新为 83 项测试、类型检查和构建通过。M0 尚缺内置全量对照、持久化全局/Workspace 启停、更新结果读取、Remote 放置及多窗口并发等证据，不能整项勾选。

来源：[PRD-000](PRD/000-overview.md)公共集成规则及 F01/F08/F09/F12/F13/F15。

- [ ] M0-01：验证当前 Profile 完整本地安装清单的可维护读取方案，逐 ID 对照原生清单，涵盖禁用、未激活、内置、VSIX 和无市场条目；不能把 `vscode.extensions.all` 返回子集当作全量。
- [ ] M0-02：验证真实启用状态、全局/Workspace 禁用范围、信任/策略/依赖限制与待重启状态的数据来源；不能使用 `isActive` 推断 enabled。
- [ ] M0-03：验证真实更新查询、已装/可用版本、单插件原生更新、原生更新列表入口，以及无市场条目和离线时的返回行为。
- [ ] M0-04：记录目标 VS Code 版本、原生命令、参数和本地 Profile 定位方式；验证直接调用与 Open in VS Code 委托各自能完成的流程。能力缺口需记录阻塞，不能假报操作成功或静默降级。
- [ ] M0-05：验证本地扩展宿主放置、Profile 隔离、普通 Workspace 切换及多窗口存储行为；连接 Remote 的窗口也不能误读/误操作远端插件。不直接依赖当前 Shell 的 `code` 指向。
- [ ] M0-06：验证原生安装/卸载、启停、后台更新的事件来源，以及面板重新聚焦和手动刷新的补读方案；确认原生树 Disabled/Unknown 可读表达的 API 边界。

## 各 PRD 剩余实现与验收

“接入”指将已有 Demo 能力接到正式模型；“验收”指保留已有实现，并在真实数据和宿主中提供证据。

### F01 · 全量安装清单

来源：[PRD-001](PRD/001-installed-inventory.md)，A01、A21 本地读取失败部分。

- [ ] F01-01【实现】：建立真实本地 Inventory 服务，以规范化 Extension ID 为主键，处理缺少图标/描述/市场记录的 ID 占位；准确读取内置/用户安装类型，不漏掉禁用和未激活插件，不主动激活其他扩展。
- [ ] F01-02【实现】：显示当前 Profile/本地范围与清单读取状态；首次不完整或失败显示 Incomplete/错误和重试，后续失败保留最后有效清单并标 Stale，不误报零安装或已卸载。
- [ ] F01-03【验收】：构建至少 50 个唯一 ID 的本地夹具，对照原生清单逐项检查数量与分类；初始未分组数量正确，Profile 切换不混入旧清单。

### F02 · 原生分组树

来源：[PRD-002](PRD/002-native-group-tree.md)，PRD-002-AC01/AC02。

- [ ] F02-01【接入】：从正式清单与唯一 assignments 构建树，用户组和 Ungrouped 互斥覆盖全部本地 ID，每个 ID 只出现一次。
- [ ] F02-02【验收】：刷新、管理操作和状态同步后尽量保留展开/选中/滚动位置；点击组正确筛选 Dashboard，重复打开只聚焦同一面板。

### F03 · 搜索与状态筛选

来源：[PRD-003](PRD/003-search-and-filters.md)，A18。

- [ ] F03-01【实现】：补齐 Built-in/User Installed 类型筛选，接入真实 Enabled/Disabled/Unknown 与更新状态；内置插件默认不静默排除。
- [ ] F03-02【接入/验收】：真实数据下验证名称、ID、publisher、Tag 搜索与分组、状态、类型、多 Tag AND 条件组合。保留统计互斥和 All 清除全部筛选的现有行为；不新增状态下拉栏、不改变归属。

### F04 · 分组管理

来源：[PRD-004](PRD/004-group-management.md)，A05、A06。

- [ ] F04-01【接入/验收】：正式配置下验证创建、改名、确认删除与取消，稳定 ID、颜色和顺序不因改名丢失；删除组后成员进入 Ungrouped，安装/启停状态和 Tag 不变。
- [ ] F04-02【验收】：真实宿主覆盖 trim、空白、大小写重复、50 个 Unicode 字符上限、Ungrouped 保留名、特殊字符纯文本显示及取消不写入。

### F05 · 唯一归属与批量拖动

来源：[PRD-005](PRD/005-single-group-assignment.md)，A02、A04。

- [ ] F05-01【接入】：正式 assignments 服务保障每个 ID 至多一个有效组；单选/Ctrl 多选拖动一次提交，移入新组替换原归属，Tag 不变，失败不部分移动。
- [ ] F05-02【验收】：真实清单下跨组多选移动、解除归属、计数与重启恢复；不恢复 Move 按钮或弹窗。组内拖动可改变顺序，但不改变归属。

### F06 · 插件拖放

来源：[PRD-006](PRD/006-extension-drag-drop.md)，A03、A17。

- [ ] F06-01【接入/验收】：真实数据下验证 Dashboard 单选/多选、禁用卡片、组标题/成员/空组/Ungrouped 落点、同组重排、跨组插入、筛选中落点、边缘滚动、取消/失焦及拖动结束不误点击；保留当前用户认可的卡片行为。
- [ ] F06-02【验收】：单独记录原生树的组标题/成员/Ungrouped 成员、多选、失效节点和无节点空白行为；原生反馈受 VS Code API 限制，不把 Dashboard 整组高亮算作原生树已验证。
- [ ] F06-03【验收】：非法/外部拖放载荷、目标在拖动中删除或移组时不执行任意内容、不丢归属；跨 TreeView/Webview 的实际支持边界需明确记录，不声称已完整支持双向拖放。

### F07 · 分组与组内排序

来源：[PRD-007](PRD/007-group-ordering.md)，A16。

- [ ] F07-01【接入】：将组顺序、组内卡片顺序及分组颜色纳入正式组织元数据；字段排序和手动拖动统一保存，后台清单刷新不能重置用户顺序。
- [ ] F07-02【验收】：真实宿主验证六点抓手整组/间隙宽松落点、向前/向后插入、单栏/双栏和折叠组；验证组名称升降序、插件名称/publisher/ID 升降序及辅助上下移动，Ungrouped 固定，重启两视图顺序一致。

### F08 · 原生扩展页与 Copy ID

来源：[PRD-008](PRD/008-native-details.md)，A23。

- [x] F08-01【实现】：打开原生页失败时提供“在 VS Code 中查找”按钮，以固定搜索命令定位已校验的 ID；取消不执行额外命令，恢复失败传播给宿主。相关单元测试通过，真实原生导航与目标缺失失败已在隔离宿主复现；正式真实清单验收仍属 F08-02。
- [ ] F08-02【接入/验收】：图标/标题定位真实本地插件 ID，复制完整 publisher.name；补查原生树的打开/复制入口可达性，验证无市场条目、命令不支持和失败分支；原生页打开不算启停/更新成功。

### F09 · 正式本地配置与恢复

2026-09-16：新增正式 OrganizationState 解析与 Demo 组织元数据迁移函数，保留稳定组 ID、颜色、组/卡片顺序、唯一 assignments 与 tags；拒绝非法数据并区分 missing/invalid，不保存动态安装状态。PRD-009 已补齐契约。尚未切换生产宿主存储、提供恢复 UI 或解决跨窗口并发，下面各项保持未完成。

来源：[PRD-009](PRD/009-persistence.md)，A15。

- [ ] F09-01【实现】：从 Demo 全快照切换为正式 `groups`、唯一 `assignments`、`tags`，保存组/组内顺序及颜色；补齐 PRD-009 对顺序和颜色的契约，不保存动态版本、启用状态或更新结果。
- [ ] F09-02【实现】：制定并验证旧 Demo 到正式存储的迁移/隔离策略，保留已有组织数据，缺失 tags 补空数组；不把示例安装记录当成用户已安装插件。
- [ ] F09-03【实现】：损坏/未知格式/非法 Tag 读取时给恢复入口并保留原有效数据，不能静默展示 seed 后用新操作覆盖损坏原值；写入失败保留上次有效状态并可重试。
- [ ] F09-04【验收】：重启、普通 Workspace 切换、Profile 隔离、批量拖动与 Tag 写入失败的原子性；外部卸载或暂不可见时组织元数据保留策略可追溯，不用清单失败清空配置。

### F11 · 首次使用与空状态

来源：[PRD-011](PRD/011-onboarding.md)，PRD-011-AC01/AC02。

- [ ] F11-01【接入/验收】：首次无组时可点击创建；创建后空组占位立即可见并可拖入，不引导选择插件；不加载默认示例分组充当用户配置。
- [ ] F11-02【实现/验收】：分别展示尚未读取、读取失败、确认无安装、无用户组、筛选无结果，提供对应可用操作；批量选择遵循 Ctrl 点击，核心操作无需命令面板或 Shell。

### F12 · 真实启用、禁用和状态

来源：[PRD-012](PRD/012-enable-disable.md)，A07、A08；A20 延后。

- [ ] F12-01【实现】：扩展正式状态模型，区分 Enabled、Disabled、Unknown、Restart Required，展示 Workspace/全局禁用范围与信任/策略/依赖限制原因；未激活不能当禁用。
- [ ] F12-02【实现】：卡片和树接入原生启停适配层，支持原生允许的全局/Workspace 范围、取消、受限和待重启流程；操作只作用于当前 Profile 本地目标。
- [ ] F12-03【实现/验收】：处理中阻止冲突重复操作，重新读取真实结果后再显示成功；原生委托明确标识。Disabled 保留原组与 Tag，灰显仍可选、拖动和管理；完成重启后两视图与原生一致。

### F13 · 真实更新

来源：[PRD-013](PRD/013-updates.md)，A10、A11。

- [ ] F13-01【实现】：打开 Dashboard 自动查询一次真实更新，显示已装/可用版本与查询状态；版本左侧箭头执行单插件原生更新。不恢复 Check updates 按钮。
- [ ] F13-02【实现】：提供打开原生更新列表的可点击入口，保留原生渠道、兼容性、依赖和重启流程；不自行下载安装、不改自动更新设置。
- [ ] F13-03【实现/验收】：离线、网络/权限失败、无市场条目、取消时保留清单/归属/Tag，提示原因和重试/原生入口，不假报最新版；单次与后台自动更新完成后真实版本、状态和两视图同步。

### F15 · 同步与并发

来源：[PRD-015](PRD/015-state-sync.md)，A09、A19、A24。

- [ ] F15-01【实现】：建立宿主统一的真实状态源，监听原生安装、外部卸载、启停及后台更新；两种 UI 只调用服务，不维护第二份分类数据。
- [ ] F15-02【实现】：操作完成重读、事件触发同步、面板重新聚焦补读和手动刷新；显示更新时间/过期状态与失败重试，不能只根据点击改变图标。
- [ ] F15-03【实现/验收】：原生管理冲突互斥/串行，取消和失败可恢复；实现并测试跨窗口冲突处理，不能把单个宿主 actionQueue 当作多窗口保护。
- [ ] F15-04【验收】：原生页外部修改与 Nest 双向操作后，两视图和原生一致；可用通知后更新目标小于 2 秒，漏事件恢复路径有证据。

### F16 · 正式 Dashboard

来源：[PRD-016](PRD/016-dashboard.md)，A22。

- [ ] F16-01【接入】：以真实清单驱动分组卡片与统计，明确当前 Profile 本地范围和计数口径；Enabled/Disabled/Unknown 互斥，Updates 可交叉，未知更新不可计作无更新，Ungrouped 数量正确。
- [ ] F16-02【实现】：补齐加载、同步失败、处理中、重启、过期和重试反馈，以及真实 Refresh/原生 Extensions 入口；保持紧凑顶栏，不恢复重复状态下拉或 Check updates 按钮。
- [ ] F16-03【验收】：真实数据下验证组分区默认展开、空组、单/双栏响应式、全部展开/收起、Tag 同行、按钮/菜单可达、原生页点击范围、图标回退、主题色和禁用可读性；移组只通过拖动，不恢复复选框或重复分类文本。

### F17 · Tag

来源：[PRD-017](PRD/017-tags.md)，A25。

- [ ] F17-01【接入】：将现有单个编辑、批量替换、规范化和筛选接到真实 ID 与正式 tags 存储；编辑只改标签，移组/启停/更新不改标签。
- [ ] F17-02【验收】：真实宿主完成 Ctrl 多选后的批量编辑端到端测试，提交前明确目标数量及完整替换数组；取消、失败、空白去除、大小写去重、30 个 Unicode 字符/10 项上限、非法数据和旧状态补全都不部分写入。
- [ ] F17-03【验收】：多 Tag AND 与搜索、组、真实状态和安装类型条件组合；清除恢复完整本地清单，移动到其他组或 Ungrouped 后标签不变，标签数量不影响分组计数。

## 公共质量与验收收尾

来源：[PRD-000](PRD/000-overview.md)质量目标。

- [ ] QA-01：建立正式验收记录，关联 F01–F09、F11–F13、F15–F17 的实现、环境、操作和结果；覆盖 A01–A11、A15–A19、A21 的本地部分、A22–A25，以及 PRD-002/011 的 AC01/AC02。A20 和 A21 的 Remote 部分仅记延后，不标通过。
- [ ] QA-02：性能夹具至少 100 个本地唯一 ID、20 个组、最多 100 个 assignments；30 次预热展开数据准备 p95<100 ms，分组反馈<200 ms，本地快照就绪后首屏<1 s；记录硬件、VS Code 版本和测量过程，网络耗时分开呈现。原 150 个跨目标实例场景延后。
- [ ] QA-03：Light/Dark/High Contrast 实测卡片、抓手/插入线、Tag 菜单、焦点和 Disabled/Unknown/Restart Required；状态不能只靠颜色，禁用项仍可操作。
- [ ] QA-04：Windows 真实宿主完成全流程；macOS/Linux 本地场景及键盘辅助、Command 多选补测。未验证平台明确限制，不把 WSL/SSH Remote 聚合列入首版。
- [ ] QA-05：正式适配后复核 Webview CSP/资源范围、消息白名单、ID/动作/目标校验及拖放载荷；名称/描述/Tag 纯文本渲染，拒绝任意命令/URI，不读取或修改 VS Code 私有数据库。
- [ ] QA-06：文档校准：将最新用户决定同步到仍有旧描述的 PRD-000/006/013/016/017、PRODUCT 和 Roadmap（Local-only、Ctrl 多选、移组仅拖动、组内可重排、无自建 Details、自动查更新、空组无引导）；移除过时版本/publisher 待定文字，防止下次照旧 PRD 恢复已删功能。

## 交付准备

- [ ] REL-01：publisher Lazenca 和图标已完成；确认正式扩展包名/显示名（当前仍有 Demo 标识）、License（当前 UNLICENSED）及最低支持 VS Code 版本（当前 ^1.137.0），以实测兼容范围为准。
- [ ] REL-02：正式入口去除示例清单、模拟管理和 Reset demo；保留独立开发演示方式，不能让正式用户误操作 Demo 后以为修改了真实插件。
- [ ] REL-03：整理正式安装、使用、原生管理委托、故障恢复、仅本地范围和已知限制说明；README 后续按用户要求维护。核对打包仍引用的 Demo 安装说明、授权及市场元数据。
- [ ] REL-04：功能完成后重新构建 VSIX，在干净 Profile 验证安装/激活、旧版本升级、组织数据保留、图标与离线回退；检查包内容及构建产物。当前 VSIX 不代表最新未提交修改。

提交、版本更新、CHANGELOG 和 Marketplace 发布均按用户后续明确授权执行，不因完成此清单自动进行。

## 明确延后：不阻塞本地首版

- [ ] LATER-01：当前连接 Remote 的清单聚合、Local/Remote 实例定位与选择、Mixed 状态、不同版本与实例计数、Remote 断连恢复及宿主/持久化隔离；对应 A20、A21 Remote 部分，以及原 150 实例性能和 WSL/SSH 远端验收。
- [ ] LATER-02：P1 收藏、自定义图标、Settings Sync、批量启停、自选其他 Profile 或未连接 Remote 管理。
- [ ] LATER-03：P2 规则分类、Smart Groups、AI 分类、Extension Pack/工作区推荐生成、使用统计和未使用检测；分类结果仍须唯一归属。

## 覆盖检查

本清单涵盖 15 个有效功能模块（001–009、011–013、015–017）、PRD-000 公共约束与质量目标、Roadmap 交付和后续方向。010/014 是撤销占位，旧总稿与 archive 仅供追溯。已完成的 Demo 交互不重复开发，但正式数据接入和缺失验收保持待办。

```
